const name = "Lish"
const repoCount = 50

// /console.log(name + repoCount + " Value")

console.log(`my name is ${name} and my repo count is ${repoCount}`);

const gameName = new String('Lishaz')                          //declaring a string

console.log(gameName[0]);
